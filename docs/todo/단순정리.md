# 단순히 생각나는대로 아이디어 정리한 TODO
- 수행한 쿼리는 사용자 피드백을 받아 설명 임베딩 하여 vector DB 저장 하도록
- (완료) 프롬프트 안쓰는거 , System/User 구분 , 프롬프트 퀄리티 전체 분석
- (완료) 테스트 디렉토리 및 시나리오 정리
- (완료) 두개의 그래프 공존하는 구조 확인하고 개선해야 함
- (완료, 조치중) 현재 reasoning 파이프라인이 폐쇄망 제공 정보를 기반으로 검토했을 때 적정하게 설계되었는지 비판적인 검토
- golden_set 평가 방법에 대한 구조적인 개선안.. 내부에서는 골든셋 만들기가 쉽지않기도하고, 골든셋이 저렇게 구성되는게 맞는건지 ? , 그냥 LLM 이 스코어를 판단하도록 하는 방법은 ?
- (완료) Neo4j 설치 및 온톨로지 구축 환경 구성
- (검토완료) similar_table_resolver.py — 파이프라인 미연결, 하드코딩 5개 그룹만 처리. 3측면 비교 판정 구현 시 보조 힌트 역할로 축소 예정
- (검토완료) 테이블 설명 보강(3측면) — 별도 에이전트 불필요. 기존 흐름 확장 + 비교 판정 1회 방식으로 결정. 상세: `docs/todo/table-three-aspect-enrichment.md`
- 주제영역 활용방안 (TODO 확인)
- (완료) 테이블 실제 조회할 때 스키마명 필요함 (ADW : ADWOWN / BDP : BDPOWN)
- 비판적인 검토를 토대로 한가지 더 확인하고 싶은 부분이, 현재 LLM 이 대부분의 주요한 판단을 충분히 할 수 있는 구조인지도 검토해줘. 너무 과도한 LLM 호출은 지양하지만 LLM 이 추론적으로 해야하는 부분을 Rule_based 로 처리하게 되는 문제가 정확도에 더 큰 위험이 될 것 같아.
- Tracker, Logger 구간별 적용 돼있는지 확인 필요
- 
- (검토완료, 보류) 당행의 테이블 명명규칙을 LLM에 알려줄지 — 현재 불필요 판단. 상세: `docs/todo/table-naming-convention-review.md`
- (중요)Planner 가 execution_plan 을 직접 결정하고 지시하도록 수정 (이건 AI 모델 의존성 큼)
- 현재의 response body 확인 -> Insight 정보들을 마지막에 정리해서 별도의 key:value 로 해서 전달하면 UI 에서 열어볼 수 있음
- 진행상황들도 비동기로 전달하면 UI 에서 열어볼 수 있음

(서버의 response 형태)
// response
{"type": "response", "message": "...", "visualization": {"type": "svg", "code": "...", "chart_type": "...", "title": "..."}}
// status  
{"type": "status", "message": "요청을 처리하고 있습니다..."}
// error
{"type": "error", "message": "..."}
// system (slash commands)
{"type": "system", "message": "..."}

- (중요) resolve_history 이후에 CONTINUE 인 경우에 사용자 요청을 명확화해서 결합한 후 어떤 노드로 가는게 가장 나은지 ?
  
- 테이블 비교 시점에 '사용자 질의' 를 해결하려면 놓여진 테이블들로 가능한지 확인까지, join 가능한 조합들도 확인

- 8-slot 정규화가 초기 1회 수행되지만, reasoning 과정에서 가설이 변경되면 정규화도 다시 들여다 봐야 하는 케이스가 있을지 확인 필요