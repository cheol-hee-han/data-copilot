# A-03 · 고객 세그먼트 분석

## 메타
- session_id: `e2e-a-03-76332c04`
- 경과시간: 228428.6 ms
- 실행시각: 2026-04-21T00:10:40

## 가설 (HYP)
```json
{
  "intent": "data_analysis",
  "path_contains_any": [
    "analyzer"
  ]
}
```

## 실제 (ACTUAL)
```json
{
  "intent": "data_analysis",
  "sql": " SELECT c.CUS_GRD_CD AS \"고객등급코드\",\n        CASE c.CUS_GRD_CDWHEN '01' THEN 'VIP'\n             WHEN '02' THEN '우수'\n             WHEN '03' THEN '일반'\n             WHEN '04' THEN '잠재'\n             WHEN '05' THEN '관리'\n             ELSE c.CUS_GRD_CD\n        END AS \"고객등급명\",\n        COALESCE(SUM(d.BAL_AMT),0) AS \"수신잔액합계\",\n        COALESCE(SUM(l.LN_BAL_AMT),0) AS \"여신잔액합계\",\n        ROUND(COALESCE(SUM(d.BAL_AMT),0) * 100.0 / NULLIF(COALESCE(SUM(d.BAL_AMT),0) + COALESCE(SUM(l.LN_BAL_AMT),0), 0), 2) AS \"수신비중(%)\",\n        ROUND(COALESCE(SUM(l.LN_BAL_AMT),0) * 100.0 / NULLIF(COALESCE(SUM(d.BAL_AMT),0) + COALESCE(SUM(l.LN_BAL_AMT),0), 0), 2) AS \"여신비중(%)\"\n   FROM ADWOWN.TB_ADW_CSC101M c\n   LEFT JOIN ADWOWN.TB_ADW_DEP201P d\n     ON c.EDPS_CSN = d.EDPS_CSN\n    AND d.STD_DT = CURRENT_DATE\n   LEFT JOIN ADWOWN.TB_ADW_LNB301M l\n     ON c.EDPS_CSN = l.EDPS_CSN\n    AND l.STD_DT = CURRENT_DATE\n  WHERE c.STD_DT = CURRENT_DATE\n  GROUP BY c.CUS_GRD_CD\n  ORDER BY c.CUS_GRD_CD\n  LIMIT 1000",
  "viz": "horizontal_bar",
  "rows": 182,
  "node_path": [
    "turn_reset",
    "intent_classifier",
    "query_normalizer",
    "reasoning_preparer",
    "context_retriever",
    "context_interpreter",
    "readiness_gate",
    "sql_generator",
    "sql_validator",
    "result_finalizer",
    "sql_executor",
    "analyzer",
    "visualizer",
    "formatter",
    "save_turn_snapshot"
  ],
  "route": "",
  "loop_guard": {
    "replan_count": 0,
    "local_fix_count": 0,
    "ask_user_count": 0,
    "tool_calls": 2
  },
  "awaiting_clarification": false,
  "has_clarification_request": false,
  "error": false,
  "cancelled": false,
  "response_len": 1391
}
```

## 차이 (DIFF)
(일치)

## 판정 (VERDICT)
- **결과**: PASS
- **등급**: -

## 턴 요약
### 턴 1
- **Query**: 고객 등급별로 여신·수신 비중 분석해줘
- **Response** (trim 500): {"summary":"고객 등급별 여신 및 수신 잔액 비중을 분석한 결과, 수신 비중은 0%에서 100%까지 매우 다양하고 여신 비중도 0%에서 100%까지 폭넓게 분포합니다. 전체 182개 고객 등급 중 상당수가 여신 비중 50% 이상으로 여신 중심 포트폴리오가 우세한 특징을 보입니다.","initial_reading":["최대 수신 비중은 100%로 CUSGR0013, CUSGR0036 등 여러 등급에서 나타납니다.","최대 여신 비중은 100%로 CUSGR0004, CUSGR0010, CUSGR0020 등 일부 등급에서 관찰됩니다.","최대 수신 잔액은 CUSGR0049 등급으로 약 4.69조원, 최대 여신 잔액 역시 CUSGR0049 등급으로 약 11.32조원입니다.","전체 182개 고객 등급 중 여신 비중 50% 이상인 등급 수는 83개, 수신 비중 50% 이상인 등급 수는 28개입니다."],"insights":["여신 비중이 70% 이상인 고객 등급이 다수 존재해 여신 
- **awaiting_clarification**: False
- **intent**: 
- **viz**: VisualizationType.HORIZONTAL_BAR
- **rows**: 182
- **node_path**: ['맥락분류', '질의정규화', 'SQL실행', '분석', '시각화', '포맷팅']


## 아티팩트
- trace: `tests\reports\e2e_2026Q2\traces\A-03.trace.json`
- state: `tests\reports\e2e_2026Q2\traces\A-03.state.json`
- log: `tests\reports\e2e_2026Q2\logs\A-03.log`
- events: `tests\reports\e2e_2026Q2\events\A-03.events.jsonl`

## 관찰성 지적
(자동 탐지 없음 — 필요 시 수기 추가)
