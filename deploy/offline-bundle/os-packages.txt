# ──────────────────────────────────────────────────────────────
# os-packages.txt — 폐쇄망 타겟에 설치할 RPM 목록
#
# build.sh 의 `dnf download --resolve` 로 의존성 포함 수집됨.
# 배포 대상 OS: Rocky Linux 9 / RHEL 9 계열 가정.
# 빈 줄과 '#' 로 시작하는 주석은 무시된다.
# ──────────────────────────────────────────────────────────────

# ── ODBC (Sybase IQ, SAP ODBC 드라이버 연동용) ──
unixODBC
unixODBC-devel

# ── Kerberos / SASL (Impala·Hive LDAP/GSSAPI 인증) ──
krb5-libs
krb5-workstation
cyrus-sasl
cyrus-sasl-gssapi
cyrus-sasl-plain

# ── PostgreSQL 클라이언트 라이브러리 (psycopg 등) ──
libpq
postgresql

# ── 빌드 도구 (일부 C 확장 wheel 재컴파일이 필요한 경우 대비) ──
gcc
make
openssl-devel
libffi-devel

# ── 기타 런타임 편의 ──
tar
rsync
